{
  "name": "saas-web-app",
  "description": "Multi-user web application with production-oriented defaults.",
  "recommendedRuntime": "python",
  "defaultModules": ["fastapi", "postgres", "alembic"],
  "recommendedModules": ["htmx"],
  "requiredPolicies": ["security-baseline", "ci", "privacy-baseline", "performance-baseline"],
  "rules": [
    {
      "level": "warning",
      "when": {
        "modulesAny": ["sqlite"]
      },
      "message": "SQLite is usually not a good primary database for a SaaS profile."
    }
  ]
}
