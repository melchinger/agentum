{
  "name": "desktop-app",
  "description": "Cross-platform desktop application with local-first defaults.",
  "recommendedRuntime": "rust",
  "defaultModules": ["tauri", "react", "sqlite"],
  "requiredPolicies": ["security-baseline"],
  "rules": [
    {
      "level": "info",
      "when": {
        "modulesMissing": ["sqlite"]
      },
      "message": "Desktop apps often benefit from a local-first storage module."
    },
    {
      "level": "info",
      "when": {},
      "message": "For a Svelte frontend use the desktop-app-svelte (bare SPA) or desktop-app-sveltekit (routed, prerendered) profile instead — mixing this profile with svelte modules ships both react and svelte."
    }
  ]
}
