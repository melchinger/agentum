### Python Overlay
- Keep package modules small and typed; prefer explicit dataclasses, protocols, or typed models over unstructured dictionaries.
- Separate domain logic from framework, CLI, or persistence code.
- Centralize configuration loading and validate environment-derived settings before use.
- Use virtual environments and deterministic tooling; avoid implicit global interpreter assumptions.
