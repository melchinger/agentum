### React Overlay
- Keep components presentational when possible and move data access into hooks or services.
- Do not call `fetch` or third-party clients directly inside UI components.
- Distinguish client-only state from server state; cache remote data intentionally.
- Use accessible routing, semantic landmarks, and predictable loading or error feedback for every async view.
