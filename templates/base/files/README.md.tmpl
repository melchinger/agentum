# {{PROJECT_NAME}}

Generated with `agentum` using the `{{VARIANT}}` variant.

## Getting Started

- Read `AGENTS.md` before making architecture or tooling changes.
- Copy `.env.example` to your local environment file and fill in real values outside version control.
- Use the commands documented in `AGENTS.md` for day-to-day development and validation.

## Structure

- `src/` application or library source code
- `tests/` automated tests
- `docs/` supporting documentation and checklists

## Quality Bar

- Keep changes small and well-tested.
- Preserve security defaults and typed boundaries.
- Prefer clear architecture over fast but tangled implementation.
