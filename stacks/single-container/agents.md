### Stack Module: Single Container
- Keep the container focused on one deployable unit, and document clearly which concerns still need external services.
- Do not assume a single container is enough for production if the selected storage, queues, or concurrency profile says otherwise.
