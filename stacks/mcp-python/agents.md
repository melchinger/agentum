### Stack Module: MCP (Python)
- Expose MCP tools as thin integration adapters around application services, not as direct homes for business logic.
- Keep tool inputs and outputs versioned and typed so editor integrations remain stable over time.
